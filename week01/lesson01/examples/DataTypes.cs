using System;

namespace Lesson01
{
    class DataTypes
    {
        void ExampleMethod()
        {
            // Declaración de variables: tipo nombre = valor;
            // Las variables en C# son case-sensitive
            int x = 1;
            float valorFlotante = 2f;
            double miVariableDouble = 2.5;
            decimal d = 3m;
            string cadena = "Esto es una cadena de caracteres";
            char caracter = 'c';

            // Tipos en su "versión" .NET
            Int32 entero = 1;
            Decimal valorDecimal = 3; // se puede utilizar sin la "m"
            Double valorDouble = 2;
            String otraCadena = "Esta es otra cadena";
            Char otroCaracter = 'a';
        }
    }
}
